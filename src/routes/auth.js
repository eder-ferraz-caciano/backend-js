module.exports = app => {
  // app.route('/user/salvar').post(app.src.controller.administration.user.salvar)
  // app.route('/signin').post(app.src.controller.auth.signin)
  // app.route('/validateToken').post(app.src.controller.auth.validateToken)
}

// app.route('/user/listar').all(app.config.passport.authenticate()).get(app.api.controller.administration.user.listar)
